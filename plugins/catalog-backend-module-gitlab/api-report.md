## API Report File for "@backstage/plugin-catalog-backend-module-gitlab"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { Config } from '@backstage/config';
import { EntityProvider } from '@backstage/plugin-catalog-backend';
import { EntityProviderConnection } from '@backstage/plugin-catalog-backend';
import { GitLabIntegration } from '@backstage/integration';
import { GitLabIntegrationConfig } from '@backstage/integration';
import { GroupEntity } from '@backstage/catalog-model';
import { Logger as Logger_2 } from 'winston';
import { RequestInit as RequestInit_2 } from 'node-fetch';
import { Response as Response_2 } from 'node-fetch';
import { ScmIntegrations } from '@backstage/integration';
import { UserEntity } from '@backstage/catalog-model';

// Warning: (ae-missing-release-tag) "getGroupMembers" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function getGroupMembers(
  client: GitLabClient,
  id: string,
  options?: UserIngestionOptions,
): Promise<UserEntity[]>;

// Warning: (ae-forgotten-export) The symbol "GroupAdjacency" needs to be exported by the entry point index.d.ts
// Warning: (ae-missing-release-tag) "getGroups" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function getGroups(
  client: GitLabClient,
  _id: string,
  pathDelimiter: string,
  groupType?: string,
): Promise<GroupAdjacency>;

// Warning: (ae-missing-release-tag) "getInstanceUsers" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function getInstanceUsers(
  client: GitLabClient,
  options?: UserIngestionOptions,
): Promise<UserEntity[]>;

// Warning: (ae-missing-release-tag) "GitLabClient" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export class GitLabClient {
  constructor(options: { config: GitLabIntegrationConfig; logger: Logger_2 });
  // (undocumented)
  readonly baseUrl: string;
  isSelfManaged(): boolean;
  // Warning: (ae-forgotten-export) The symbol "ListOptions" needs to be exported by the entry point index.d.ts
  // Warning: (ae-forgotten-export) The symbol "PagedResponse" needs to be exported by the entry point index.d.ts
  pagedRequest<T = any>(
    endpoint: string,
    options?: ListOptions,
  ): Promise<PagedResponse<T>>;
  request(endpoint: string, init?: RequestInit_2): Promise<Response_2>;
}

// @alpha
export class GitLabOrgEntityProvider implements EntityProvider {
  constructor(options: {
    id: string;
    logger: Logger_2;
    integrations: ScmIntegrations;
    providerConfigs: GitLabOrgProviderConfig[];
  });
  // (undocumented)
  connect(connection: EntityProviderConnection): Promise<void>;
  // (undocumented)
  static fromConfig(
    configRoot: Config,
    options: {
      id: string;
      logger: Logger_2;
    },
  ): GitLabOrgEntityProvider;
  // (undocumented)
  getProviderName(): string;
  // (undocumented)
  read(): Promise<void>;
}

// @public
export type GitLabOrgProviderConfig = {
  target: string;
  users: {
    ingest: boolean;
    exclude: string[];
    prefix: string;
  };
  groups: {
    ingest: boolean;
    exclude: string[];
    prefix: string;
    delimiter: string;
  };
};

// @public
export type GitLabUserResponse = {
  id: number;
  name: string;
  username: string;
  state: string;
  avatar_url: string;
  web_url: string;
  created_at: string;
  job_title: string;
  public_email?: string;
  email?: string;
  bot?: boolean;
  bio?: string;
  location?: string;
  skype?: string;
  linkedin?: string;
  twitter?: string;
  website_url?: string;
  organization?: string;
  followers?: number;
  following?: number;
};

// Warning: (ae-forgotten-export) The symbol "FindIntegrationByURL" needs to be exported by the entry point index.d.ts
// Warning: (ae-missing-release-tag) "groupByIntegrationConfig" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function groupByIntegrationConfig(
  byUrl: FindIntegrationByURL,
  providerConfigs: GitLabOrgProviderConfig[],
): Map<GitLabIntegrationConfig, GitLabOrgProviderConfig[]>;

// Warning: (ae-missing-release-tag) "paginated" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
// Warning: (ae-unresolved-link) The @link reference could not be resolved: The package "@backstage/plugin-catalog-backend-module-gitlab" does not have an export "pagedRequest"
//
// @public
export function paginated<T = any>(
  request: (options: ListOptions) => Promise<PagedResponse<T>>,
  options: ListOptions,
): AsyncGenerator<Awaited<T>, void, unknown>;

// Warning: (ae-missing-release-tag) "parseGitLabGroupUrl" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function parseGitLabGroupUrl(
  url: string,
  baseUrl?: string,
): null | string;

// Warning: (ae-missing-release-tag) "readGitLabOrgProviderConfig" is exported by the package, but it is missing a release tag (@alpha, @beta, @public, or @internal)
//
// @public (undocumented)
export function readGitLabOrgProviderConfig(
  config: Config,
): GitLabOrgProviderConfig[];

// @public
export function readUsers(
  client: GitLabClient,
  target: string,
  options?: UserIngestionOptions,
): Promise<UserEntity[]>;

// @public
export type UserIngestionOptions = {
  inherited?: boolean;
  blocked?: boolean;
  transformer?: UserTransformer;
};

// @public
export type UserTransformer = (
  user: GitLabUserResponse,
) => Promise<UserEntity | undefined>;
```
